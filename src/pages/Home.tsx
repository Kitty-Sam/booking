import { Header } from '@components/Header';
import { MainPanel } from '@components/MainPanel';

export const Home = () => {
    return (
        <>
            <Header />
            <MainPanel />
        </>
    );
};
