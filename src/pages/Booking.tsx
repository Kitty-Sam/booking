import { Header } from '@components/Header';
import { MainPanel } from '@components/MainPanel';

export const Booking = () => {
    return (
        <>
            <Header />
            <MainPanel />
        </>
    );
};
