import './App.css';
import { Header } from '@components/Header';
import { MainPanel } from '@components/MainPanel';

export const App = () => {
    return (
        <>
            <Header />
            <MainPanel />
        </>
    );
};
