import { Header } from '@components/Header';
import { MainPanel } from '@components/MainPanel';

export const Home = () => {
    return (
        <>
            <Header />
            <div>Home</div>
            <MainPanel />
        </>
    );
};
