import { Header } from '@components/Header';
import { MainPanel } from '@components/MainPanel';

export const Profile = () => {
    return (
        <>
            <Header />
            <div>Profile</div>
            <MainPanel />
        </>
    );
};
